language: python
python:
  - 2.7
virtualenv:
  system_site_packages: true
install:
  - sudo apt-get update
  - sudo apt-get install python-numpy python-gdal
  - pip install basemap
  - pip install .
script: nosetests